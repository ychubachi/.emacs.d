#+TITLE: config.el
#+AUTHOR: Yoshihide Chubachi
#+PROPERTY: header-args :tangle "init.old.el"

* はじめに
** コメント
+ このファイルはかつて init.el を org-babel tangle で生成するための元ファイルだった
+ 現在は lisp/*.el に完全移植済みで、このファイル自体は使われていない（[[FILE:README.org][README.org]] 参照）
+ 2026-09-23、移植済みの設定はすべて削除した（過去の内容はgit履歴で参照可能）

** 参考になるサイト
- [[http://xahlee.info/emacs/emacs/elisp.html][Practical Emacs Lisp]]

** 事前にインストールしておくもの
- 各種フォント
  - Noto Sans Mono CJK JP-12
- migemo
  - cmigemo
- pygment
  - python3をインストール
  - /home/yc/.local/binにパス
  - pip install -U pip
  - pip install pygments
- graphviz(dot)
  - sudo apt install graphviz
- org-roam
  - sqlite3

* 未移植 - migemo
- ローマ字での日本語インクリメンタルサーチ。cmigemo(apt)の導入が必要で未対応（README.org参照）

#+begin_src emacs-lisp
  (use-package migemo
    :when (eq system-type 'gnu/linux)
    :custom
    (migemo-command . "cmigemo")
    (migemo-options .'("-q" "--emacs"))
    (migemo-dictionary . "/usr/share/cmigemo/utf-8/migemo-dict")
    (migemo-user-dictionary . nil)
    (migemo-regex-dictionary . nil)
    (migemo-coding-system . 'utf-8-unix)
    :defun (migemo-init)
    :config
    (migemo-init))
#+end_src

* TODO やること
+ recentfがおかしい？
+ C-c C-x C-l(org-show-all)がきかない
+ [[https://github.com/misohena/org-cmenu][misohena/org-cmenu: Context Sensitive Menu for Emacs Org Mode]]
